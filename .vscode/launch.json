
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "React-debugger",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}